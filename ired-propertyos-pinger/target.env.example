# Example local environment values for running ping.sh manually.
# GitHub Actions sets TARGET_URL inside .github/workflows/ired-propertyos-ping.yml.

TARGET_URL=https://ired-propertyos-monorepo.onrender.com
MAX_RETRIES=3
CONNECT_TIMEOUT_SECONDS=10
MAX_TIME_SECONDS=30
RETRY_DELAY_SECONDS=10
